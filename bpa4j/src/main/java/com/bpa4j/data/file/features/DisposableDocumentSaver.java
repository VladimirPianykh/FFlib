package com.bpa4j.data.file.features;

import com.bpa4j.defaults.features.transmission_contracts.DisposableDocument;
import com.bpa4j.feature.FeatureSaver;

/**
 * Saver for DisposableDocument feature.
 * @author AI-generated
 */
@SuppressWarnings("rawtypes")
public class DisposableDocumentSaver implements FeatureSaver<DisposableDocument>{
	public void save(DisposableDocument f){
		// DisposableDocument doesn't persist data
	}
	
	public void load(DisposableDocument f){
		// DisposableDocument doesn't persist data
	}
}
